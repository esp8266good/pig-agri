# CONTEXT

這份檔案是 pig-agri 的詞彙表，只定義名詞。實作細節、決策理由、待辦事項都不放這裡；
那些屬於 `docs/superpowers/specs/` 與 `docs/superpowers/plans/`。

## 豬隻與軌跡

**tracklet**
MOT 追蹤器從「發現一個目標」到「跟丟」之間產生的那一段軌跡，識別碼是 `object_id`。
⚠ 一個 `object_id` 不等於一隻豬：同一隻豬被擋住一次再出來就換一個新號碼，
所以一隻豬一天會留下好幾段 tracklet，而舊的那些從此再也不會出現在畫面上。
活動量、異常判定、關注清單的成員，單位全部都是 tracklet 而不是豬。

**在畫面上**
一段 tracklet 最後一次被偵測到的時刻距今不超過一小段容忍時間。
它跟「這隻豬還在豬舍裡」是兩件事：豬沒走，只是牠的 tracklet 死了。

**離開畫面**
一段 tracklet 曾經存在，但已經不在畫面上。因為編號會跳，
離開畫面通常不代表那隻豬離開了，只代表這個編號現在指不到畫面上任何東西。

## 豬隻與活動量

**活動量（activity rate）**
一隻豬在分析視窗內的移動速率，單位 px/s，由 bbox 中心點串成的路徑長度除以時間跨度得到。
它是判斷「這隻豬要不要採血」的唯一量化依據。

**合格豬**
在分析視窗內軌跡時間跨度達到最短門檻的豬。只有合格豬的活動量才進入基準統計；
不合格的豬既不當基準，也不會被判定為異常。

**基準（median）**
同一台相機所有合格豬的活動量中位數。異常判定一律相對於基準，不使用絕對速率門檻。

## 關注清單

**關注清單**
前端右欄呈現的一份列表，用途是回答「**現在畫面上**該去看哪幾隻豬」。
成員只有在畫面上的 tracklet，來自三種標籤，不含人工釘選。
一個點下去畫面上找不到任何框的編號，對要走進豬舍找豬的人沒有用處，
所以離開畫面的成員退到「最近消失」。

**最近消失**
關注清單底下收合的一小段，放剛剛還是異常、但 tracklet 已經死掉的編號。
它不是採血名單：採血的權威紀錄是告警，不會因為編號跳號而消失。

**異常（anomaly）**
後端狀態機判定為 `alerted` 的豬。進出完全由後端決定，前端只呈現。
一隻豬同時可能因活動量與體溫兩種原因為異常。

**最低（lowest）**
在畫面上的豬裡，活動量排名最後 N 名。純排名結果，不寫入告警紀錄、不推播、不觸發任何後端狀態變化。
只在該相機沒有任何異常時才出現。

**對照（reference）**
在畫面上的豬裡，活動量排名最前 M 名。存在的目的只有一個：給人眼一個「正常長什麼樣」的參考點。
它永遠不代表問題，因此在清單中與前兩類分開呈現。

## 遮罩

**遮罩（mask）**
畫在某一台相機畫面上的一組多邊形區域。
遮罩**不會改變任何影像**：錄影、直播、回放看到的畫面完全不受影響。
它唯一的作用是讓與它重疊過多的偵測框被丟棄，不進入追蹤。

**遮罩區域（mask region）**
遮罩的組成單位，一個多邊形。每個區域可獨立命名與啟用停用，
因此可以單獨關掉一塊來定位問題，而不必整組遮罩開關。

**停用的遮罩區域**
存在、有座標、看得見，但不丟棄任何偵測框的區域。
它蓋住的通常是固定不動的背景物件，因此也是現成的對位基準。

## 通知

**告警（alert）**
後端在豬隻進入異常狀態的那一刻寫下的一筆紀錄。一次狀態轉換寫一筆。

**告警串（alert group）**
同一台相機、同一隻豬、同一種指標的連續告警，在時間上彼此接近時，
於通知清單中合併呈現為一條，並標示發生次數。
時間上分得夠開的告警屬於不同的串，因為「今天又低了」本身就是訊號。

## 熱像對位

**對位（alignment）**
RGB 畫面上的一個位置，對應到熱像畫面上哪個位置的換算關係。
兩顆鏡頭視角不同、位置差幾公分，所以同一隻豬在兩張圖上不會落在同一處。
一台相機只有一組對位，由人用眼睛校正，校好之後長期不變。

**對位基準（alignment landmark）**
校正時用來判斷「對到了沒有」的參照物。它必須是兩張圖上都看得見、而且不會移動的東西。
豬會走動、夜間 RGB 又全黑，所以豬是很差的基準。

**對位底圖（alignment underlay）**
校正時半透明疊在熱像畫面上的那張 RGB 影像。
它讓人在同一個畫面裡看到兩顆鏡頭各自拍到的同一片背景，藉此判斷對位推得夠不夠。
它只在校正時出現，不影響錄影、直播、回放看到的畫面。

## 介面

**說明模式（help mode）**
前端的一個全域狀態。開啟時，點擊任何介面元件都只顯示該元件的說明，不執行它的動作。
它的存在是為了讓沒有 hover 的觸控裝置也能取得說明。
