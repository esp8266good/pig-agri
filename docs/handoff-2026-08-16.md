# 交接：慢速相機的追蹤與 bbox 閃爍（2026-08-16）

## 現況一句話

安全性改良已上線並驗證；追蹤方面改了「停滯門檻」但**還沒有現場證據證明有效**，
下次白天重啟收一次 log 就能定案。

## 待辦（依優先序）

### 1. 驗證 age-out 修正有沒有效 — 10 分鐘，白天做

```bash
systemctl --user restart pig-agri-tmux.service
tmux attach -t pig-agri     # 看 60 秒，找 [inference] 那行
```

判讀：只要相機還在持續送幀（哪怕 3 秒一張），`ageout` 就該是 **0**。

```
cam_03: fresh=36(0.6/s) stale=140 ageout=0 fid=...
                                  ^^^^^^^^ 這個是關鍵
```

- `ageout=0` → 修正有效，可以往下談 tracker 參數（見第 3 點）。
- `ageout` 還是幾十 → 門檻 `STALE_SECONDS_BEFORE_AGING`（現 10s）還不夠大，
  對照同一輪 `[rpi_tailscale*]` 的 `age med` 決定要調到多少。

### 2. cam_02 是硬體問題，不要再查程式

抽 2026-08-16 16:01 的畫面看過：整張被強光打爆，其餘一片混濁，**人眼也看不到
任何一隻豬**。送幀正常（0.6 fps）、HLS 段大小正常，純粹是拍不到東西。
需要有人去現場擦鏡頭或調角度避開光源。在那之前它的零筆資料是正確行為。

重抽畫面的指令：

```bash
ffmpeg -i /media/lazoark/1TB-HDD/data/pig_monitoring/hls/cam_02/rgb/<YYYY-MM-DD-HH>/seg_019.ts \
       -frames:v 1 /tmp/cam_02.jpg
```

### 3. tracker 參數是「幀數」，但相機只有 0.3~1 fps（尚未動）

`inference/tracker_pool.py` 的 `max_age=300`、`lost_track_buffer=1200`、
`track_buffer=30` 都是 **update 次數**，設定時假設 10 fps（≈30 秒 / 120 秒）。
遠端相機實際 0.3~1 fps → 變成 300~1000 秒和 1200~4000 秒，一隻豬離開後殘影
軌跡會拖十幾分鐘，影響 ID 正確性進而影響活動量。

**動它等於動追蹤行為，先做完第 1 點、有乾淨數據再談。**
⚠ `reid_revive_thresh` 寧可調小不要調大（見 CLAUDE.md）。

## 這次已完成、不用再看的

| 項目 | 狀態 |
|---|---|
| 設定值域檢查（擋 SSRF、擋 `hls_retention_days=0` 刪光錄影） | ✅ 已上線驗證 |
| Postgres / uvicorn 只綁 127.0.0.1 | ✅ 已驗證區網直連失敗、Traefik 正常 |
| 選用登入機制（`AUTH_ENABLED`，預設關閉） | ✅ 已就緒，啟用步驟見 `service-readme.md` |
| tracker future 例外不再漏取 | ✅ |
| 前端 bbox 零階保持 + 漸進淡出 | ✅ 使用者確認視覺上大幅改善 |
| `tracking_logs` 去重（7/21 已跑） | ✅ 126M → 50.4M |

## 環境提醒

- 服務 `pig-agri-tmux.service`（tmux 型，非 `--reload`），改 `.py` 要重啟才生效；
  改 `static/` 直接生效，重新整理瀏覽器即可。
- **不要**另開第二份 uvicorn 對同一批錄影目錄跑 ffmpeg（寫入衝突）。
- 不要讀 `.env`（權限會拒絕）；要設定值用 `GET /settings` 或
  `uv run python -c "from config import settings; ..."`。
- 測試基準：`uv run pytest -p no:cacheprovider` 應為 **378 passed, 0 failed**。
- `tracking_logs_old`（22 GB 備份表）還在，確認無誤後可自行 DROP，不急。

## 相關文件

- 凍結重灌的根因與 DB 汙染始末：`docs/handoff-tracking-gap-2026-07-20.md`
- 登入啟用步驟：`service-readme.md` 的「啟用帳號密碼登入」
